
import { FileRecord } from '../types';

/**
 * Starting database for the MVP.
 * Initialized as an empty array to allow users to populate the system 
 * exclusively with their own documents and folder structures.
 */
export const INITIAL_FILES: FileRecord[] = [];
